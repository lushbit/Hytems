Group {
  LayoutMode: Left;
  Anchor: (Height: 30);
  Padding: (Bottom: 4);

  Label #SourceName {
    Text: "";
    FlexWeight: 1;
    Style: (
      FontSize: 12,
      TextColor: #ffffff,
      VerticalAlignment: Center,
      RenderBold: true
    );
  }

  Group {
    Anchor: (Width: 4);
  }

  Group #ZoneBadges {
    LayoutMode: Left;
  }
}
